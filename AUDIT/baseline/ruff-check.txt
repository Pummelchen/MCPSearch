EXE001 Shebang is present but file is not executable
 --> scripts/mcp_smoke.py:1:1
  |
1 | #!/usr/bin/env python3
  | ^^^^^^^^^^^^^^^^^^^^^^
2 | """End-to-end smoke test for the SwiftWebSearchMCP stdio server.
  |

BLE001 Do not catch blind exception: `Exception`
   --> scripts/mcp_smoke.py:165:16
    |
163 |         try:
164 |             return self.process.stderr.read()
165 |         except Exception:  # pragma: no cover - best effort in a failure path
    |                ^^^^^^^^^
166 |             return "<unavailable>"
    |

UP041 [*] Replace aliased errors with `TimeoutError`
   --> scripts/mcp_smoke.py:290:20
    |
288 |             try:
289 |                 chunk = connection.recv(65536)
290 |             except socket.timeout:
    |                    ^^^^^^^^^^^^^^
291 |                 break
292 |             if not chunk:
    |
help: Replace `socket.timeout` with builtin `TimeoutError`
    |
289 |                 chunk = connection.recv(65536)
    -             except socket.timeout:
290 +             except TimeoutError:
291 |                 break
    |

FURB167 [*] Use of regular expression alias `re.M`
   --> scripts/mcp_smoke.py:324:78
    |
322 |         rest = "".join(pieces)
323 |
324 |     messages = [json.loads(m) for m in re.findall(r"^data: (\{.*\})$", rest, re.M)]
    |                                                                              ^^^^
325 |     if not messages and rest.strip().startswith("{"):
326 |         messages = [json.loads(rest)]
    |
help: Replace with `re.MULTILINE`
    |
323 |
    -     messages = [json.loads(m) for m in re.findall(r"^data: (\{.*\})$", rest, re.M)]
324 +     messages = [json.loads(m) for m in re.findall(r"^data: (\{.*\})$", rest, re.MULTILINE)]
325 |     if not messages and rest.strip().startswith("{"):
    |

EXE001 Shebang is present but file is not executable
 --> scripts/monitor_tty_smoke.py:1:1
  |
1 | #!/usr/bin/env python3
  | ^^^^^^^^^^^^^^^^^^^^^^
2 | """Drive the mcps-mon interactive display through a pseudo-terminal.
  |

RUF100 [*] Unused `noqa` directive (non-enabled: `N802`)
   --> scripts/monitor_tty_smoke.py:137:40
    |
135 |     def _handler(payload: bytes) -> type[http.server.BaseHTTPRequestHandler]:
136 |         class Handler(http.server.BaseHTTPRequestHandler):
137 |             def do_GET(self) -> None:  # noqa: N802 (stdlib naming)
    |                                        ^^^^^^^^^^^^
138 |                 self.send_response(200)
139 |                 self.send_header("Content-Type", "application/json")
    |
help: Remove unused `noqa` directive
    |
136 |         class Handler(http.server.BaseHTTPRequestHandler):
    -             def do_GET(self) -> None:  # noqa: N802 (stdlib naming)
137 +             def do_GET(self) -> None:
138 |                 self.send_response(200)
    |

F541 [*] f-string without any placeholders
   --> scripts/monitor_tty_smoke.py:371:15
    |
369 |         require("final:" in ANSI.sub("", tail), "the run should end with its summary")
370 |
371 |         print(f"  startup hid the cursor and cleared the screen")
    |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
372 |         print(f"  frames home the cursor and clear to the end of the line and screen")
373 |         print(f"  colour on by default; `c` switched it off ({len(session.frames())} frames painted)")
    |
help: Remove extraneous `f` prefix
    |
370 |
    -         print(f"  startup hid the cursor and cleared the screen")
371 +         print("  startup hid the cursor and cleared the screen")
372 |         print(f"  frames home the cursor and clear to the end of the line and screen")
    |

F541 [*] f-string without any placeholders
   --> scripts/monitor_tty_smoke.py:372:15
    |
371 |         print(f"  startup hid the cursor and cleared the screen")
372 |         print(f"  frames home the cursor and clear to the end of the line and screen")
    |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
373 |         print(f"  colour on by default; `c` switched it off ({len(session.frames())} frames painted)")
374 |         print(f"  `e` hid and restored the engine breakdown")
    |
help: Remove extraneous `f` prefix
    |
371 |         print(f"  startup hid the cursor and cleared the screen")
    -         print(f"  frames home the cursor and clear to the end of the line and screen")
372 +         print("  frames home the cursor and clear to the end of the line and screen")
373 |         print(f"  colour on by default; `c` switched it off ({len(session.frames())} frames painted)")
    |

F541 [*] f-string without any placeholders
   --> scripts/monitor_tty_smoke.py:374:15
    |
372 |         print(f"  frames home the cursor and clear to the end of the line and screen")
373 |         print(f"  colour on by default; `c` switched it off ({len(session.frames())} frames painted)")
374 |         print(f"  `e` hid and restored the engine breakdown")
    |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
375 |         print(f"  `r` repainted immediately")
376 |         print(f"  `p` probed providers and the stub answered OK")
    |
help: Remove extraneous `f` prefix
    |
373 |         print(f"  colour on by default; `c` switched it off ({len(session.frames())} frames painted)")
    -         print(f"  `e` hid and restored the engine breakdown")
374 +         print("  `e` hid and restored the engine breakdown")
375 |         print(f"  `r` repainted immediately")
    |

F541 [*] f-string without any placeholders
   --> scripts/monitor_tty_smoke.py:375:15
    |
373 |         print(f"  colour on by default; `c` switched it off ({len(session.frames())} frames painted)")
374 |         print(f"  `e` hid and restored the engine breakdown")
375 |         print(f"  `r` repainted immediately")
    |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
376 |         print(f"  `p` probed providers and the stub answered OK")
377 |         print(f"  `q` exited 0, restored the cursor and printed the summary")
    |
help: Remove extraneous `f` prefix
    |
374 |         print(f"  `e` hid and restored the engine breakdown")
    -         print(f"  `r` repainted immediately")
375 +         print("  `r` repainted immediately")
376 |         print(f"  `p` probed providers and the stub answered OK")
    |

F541 [*] f-string without any placeholders
   --> scripts/monitor_tty_smoke.py:376:15
    |
374 |         print(f"  `e` hid and restored the engine breakdown")
375 |         print(f"  `r` repainted immediately")
376 |         print(f"  `p` probed providers and the stub answered OK")
    |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
377 |         print(f"  `q` exited 0, restored the cursor and printed the summary")
378 |     finally:
    |
help: Remove extraneous `f` prefix
    |
375 |         print(f"  `r` repainted immediately")
    -         print(f"  `p` probed providers and the stub answered OK")
376 +         print("  `p` probed providers and the stub answered OK")
377 |         print(f"  `q` exited 0, restored the cursor and printed the summary")
    |

F541 [*] f-string without any placeholders
   --> scripts/monitor_tty_smoke.py:377:15
    |
375 |         print(f"  `r` repainted immediately")
376 |         print(f"  `p` probed providers and the stub answered OK")
377 |         print(f"  `q` exited 0, restored the cursor and printed the summary")
    |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
378 |     finally:
379 |         session.close()
    |
help: Remove extraneous `f` prefix
    |
376 |         print(f"  `p` probed providers and the stub answered OK")
    -         print(f"  `q` exited 0, restored the cursor and printed the summary")
377 +         print("  `q` exited 0, restored the cursor and printed the summary")
378 |     finally:
    |

BLE001 Do not catch blind exception: `Exception`
   --> scripts/monitor_tty_smoke.py:395:12
    |
393 |         print(f"MONITOR TTY SMOKE FAILED: {error}", file=sys.stderr)
394 |         return 1
395 |     except Exception as error:  # pragma: no cover - surfaced rather than swallowed
    |            ^^^^^^^^^
396 |         print(f"MONITOR TTY SMOKE ERRORED: {error!r}", file=sys.stderr)
397 |         return 1
    |

EXE001 Shebang is present but file is not executable
 --> scripts/searxng_health.py:1:1
  |
1 | #!/usr/bin/env python3
  | ^^^^^^^^^^^^^^^^^^^^^^
2 | """Health check for a SearXNG instance used as an MCP search provider.
  |

EXE001 Shebang is present but file is not executable
 --> scripts/soak.py:1:1
  |
1 | #!/usr/bin/env python3
  | ^^^^^^^^^^^^^^^^^^^^^^
2 | """Volume soak test for SwiftWebSearchMCP.
  |

S110 `try`-`except`-`pass` detected, consider logging the exception
   --> scripts/soak.py:194:9
    |
192 |           try:
193 |               self.process.stdin.close()
194 | /         except Exception:
195 | |             pass
    | |________________^
196 |           try:
197 |               self.process.wait(timeout=30)
    |

BLE001 Do not catch blind exception: `Exception`
   --> scripts/soak.py:194:16
    |
192 |         try:
193 |             self.process.stdin.close()
194 |         except Exception:
    |                ^^^^^^^^^
195 |             pass
196 |         try:
    |

FURB188 [*] Prefer `str.removeprefix()` over conditionally replacing with slice.
   --> scripts/soak.py:241:9
    |
239 |           if not line or line.startswith("#"):
240 |               continue
241 | /         if line.startswith("export "):
242 | |             line = line[len("export "):]
    | |________________________________________^
243 |           if "=" not in line:
244 |               continue
    |
help: Use removeprefix instead of assignment conditional upon startswith.
    |
240 |             continue
    -         if line.startswith("export "):
    -             line = line[len("export "):]
241 +         line = line.removeprefix("export ")
242 |         if "=" not in line:
    |

Found 18 errors.
[*] 10 fixable with the `--fix` option.
